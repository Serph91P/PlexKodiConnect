# Test fixtures

Place sample Plex API XML payloads, MediaContainer responses, and DB snapshots
here. Each fixture should be a minimal, anonymized excerpt that exercises one
behavior. Filename convention:

    <area>_<scenario>.xml      e.g. movie_with_extras.xml
    <area>_<scenario>.json
