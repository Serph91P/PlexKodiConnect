name: Add-on Validations

on:
  push:
    branches: [main, develop]

  pull_request:
    branches: [main, develop]

jobs:
  addon-validations:
    name: Add-on Validations
    runs-on: ubuntu-latest

    steps:
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Checkout Add-on
        uses: actions/checkout@v4
        with:
          path: plugin.video.plexkodiconnect

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install git+https://github.com/xbmc/addon-check.git

      - name: Kodi Add-on Checker (Omega)
        id: kodi-addon-checker
        run: |
          kodi-addon-checker plugin.video.plexkodiconnect --branch=omega
